package com.towerdefence.engine.model;

public enum AbilityId {
    /** Multiplies coin income for a short burst. */
    GOLDEN_TOWER,
    /** Instant damage to everything on the lane. */
    DEATH_WAVE,
    /** Hits a handful of enemies at once. */
    CHAIN_LIGHTNING,
    /** Drags enemies back down the lane and slows them. */
    BLACK_HOLE
}
