package com.towerdefence.engine.session;

import com.towerdefence.engine.model.RunPhase;

import java.util.List;

public record GameSnapshot(
        RunPhase phase,
        int wave,
        int bestWave,
        int tier,
        int highestTierUnlocked,
        int bestWaveInTier,
        int coresPerMilestone,
        int wavesPerCore,
        int wavesToNextCore,
        int wavesPerSigil,
        int wavesToNextSigil,
        boolean eliteWave,
        double coins,
        double shards,
        double cores,
        double sigils,
        double crests,
        double stars,
        int pendingStars,
        double pendingShards,
        double towerHp,
        double towerMaxHp,
        double wall,
        double wallMax,
        double regenPerSecond,
        double damage,
        double fireInterval,
        double range,
        int multishot,
        double critChance,
        int bounces,
        double thornsDps,
        double gameSpeed,
        double goldenRemaining,
        double blackHoleRemaining,
        boolean autoCast,
        boolean autoCastUnlocked,
        boolean autoBuy,
        boolean autoUnlocked,
        boolean autoRun,
        boolean autoRunUnlocked,
        int autoRunTier,
        double offlineHours,
        AutoPrestigeRule autoPrestige,
        boolean autoPrestigeUnlocked,
        boolean formulaUnlocked,
        String formulaError,
        int runsCompleted,
        List<EnemyView> enemies,
        List<ProjectileView> projectiles,
        List<UpgradeView> attack,
        List<UpgradeView> defense,
        List<UpgradeView> meta,
        List<UpgradeView> permanents,
        List<UpgradeView> automation,
        List<AbilityView> abilities,
        List<LabView> labs,
        List<TierView> tiers,
        int wavesPerTier,
        List<MissionView> missions,
        List<UpgradeView> contracts,
        List<UpgradeView> prestige
) {}
