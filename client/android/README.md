# Android client

Placeholder. The game logic lives in `engine` and rendering in `client/core`, so this module can wrap the same game later.

Android still lags on Java 25 bytecode, so desktop is the iteration target for now.
