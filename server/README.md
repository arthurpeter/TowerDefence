# Server

Not part of the current build. The game is fully offline.

Later this becomes Spring Boot 4 + WebFlux (Netty) + Kafka + Redis + Postgres, reusing `engine` as the authoritative simulation. The game loop will not run on the Netty event loop.
